{
  "name": "cwe-plugin-player-favorites",
  "version": "0.1.0",
  "description": "CasinoWebEngine plugin template \u2014 a real, tested example plugin (player favorites). Copy, rename, build yours.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cwe-platform/plugin-sdk": "^0.1.0"
  },
  "peerDependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1",
    "zod": "^3.23.8",
    "@types/node": "^20.16.5"
  }
}
